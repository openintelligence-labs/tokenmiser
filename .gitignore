# architecture docs (uncooked — kept local until polished)
docs/

# Claude Code config (local-only)
.claude/

# CLAUDE.md (uncooked — kept local until polished)
CLAUDE.md

target/
Cargo.lock.user
*.pdb
.DS_Store
.idea/
.vscode/
.env
.env.local
*.log
.claude/settings.local.json

# fastembed model cache (downloaded on first L2 cache init)
.fastembed_cache/
# smoke-test config files (transient, generated per-session)
config.smoke.yaml
